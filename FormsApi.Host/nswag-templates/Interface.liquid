{%- if HasBaseDiscriminator %}{% if IsAbstract %}{%- assign IsDiscriminatedUnion = true -%}
{%- else -%}{% assign IsDiscriminatedUnionMember = true %}{%  endif %}{%- endif -%}
{%- if HasDescription -%}
/** {{ Description }} */
{%- endif -%}
{% if IsDiscriminatedUnion -%}
export type {{ ClassName }} = {{ DerivedClasses |  map: "ClassName" |  join: " | " }};
{%- endif -%}
{%- if ExportTypes %}{% unless IsDiscriminatedUnion %}export {% endunless %}{% endif %}interface {{ ClassName }}{% if IsDiscriminatedUnion %}_Interface{% endif %}{{ Inheritance }}{% if HasBaseDiscriminator and HasInheritance %}_Interface{% endif %} {
{%- if IsDiscriminatedUnionMember -%}
    {{ BaseDiscriminator }}: '{{ DiscriminatorName }}';
{%- endif -%}
{%- for property in Properties -%}
{%-     if property.HasDescription -%}
    /** {{ property.Description }} */
{%-     endif -%}
    {% if property.IsReadOnly %}readonly {% endif %}{{ property.InterfaceName }}{% if property.IsOptional %}?{% endif %}: {{ property.Type }}{{ property.TypePostfix }};
{%- endfor -%}
{%- if HasIndexerProperty -%}

    [key: string]: {{ IndexerPropertyValueType }};
{%- endif -%}
}
{%- if GenerateTypeCheckFunctions and HasInheritance -%}

{% if ExportTypes %}export {% endif %}function is{{ ClassName }}(object: any): object is {{ ClassName }} {
    return object && object['{{ BaseDiscriminator }}'] === '{{ DiscriminatorName }}';
}
{%- endif -%}
